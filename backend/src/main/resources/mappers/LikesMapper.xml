<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Devim.backend.repository.mybatis.LikesMapper">

    <select id="findByUserAndTarget" parameterType="map" resultType="com.Devim.backend.domain.likes.Likes">
        SELECT
            "like_no" AS likeNo,
            "user_no" AS userNo,
            "target_id" AS targetId,
            "target_type" AS targetType,
            "created_dt" AS createdDt
        FROM "likes"
        WHERE "user_no" = #{userNo}
          AND "target_id" = #{targetId}
          AND "target_type" = #{targetType}
    </select>

    <insert id="save" parameterType="com.Devim.backend.domain.likes.Likes">
        <selectKey keyProperty="likeNo" resultType="long" order="BEFORE">
            SELECT "seq_like_no".NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO "likes" ("like_no", "user_no", "target_id", "target_type", "created_dt")
        VALUES (#{likeNo}, #{userNo}, #{targetId}, #{targetType}, SYSDATE)
    </insert>

    <delete id="delete" parameterType="com.Devim.backend.domain.likes.Likes">
        DELETE FROM "likes"
        WHERE "user_no" = #{userNo}
          AND "target_id" = #{targetId}
          AND "target_type" = #{targetType}
    </delete>

</mapper>